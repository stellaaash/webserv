# This should create the relevant folders if not present, and write all logs to the file
error_log /tmp/webserv/error.log;

server {
    listen 0.0.0.0 8080;

    location / {
        root /var/www/html;
    }
}
