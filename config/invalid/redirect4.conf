server {
    listen 0.0.0.0 8080;

    location / {
        allowed_methods GET;

        root html;
    }

    location /this/is/root/ {
        allowed_methods GET;

        redirect 404 /;
    }
}
