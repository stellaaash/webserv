server {
    listen 0.0.0.0 8080;

    location / {
        allowed_methods GET;

        root html;

        # No index file means we'll get a directory index when navigating to the root of the website
        autoindex;
    }

    location /images {
        allowed_methods GET;

        root html/resources;

        autoindex on;
    }
}
